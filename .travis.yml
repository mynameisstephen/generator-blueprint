language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-blueprint' ]; then cd .. && eval "mv $currentfolder generator-blueprint" && cd generator-blueprint; fi

